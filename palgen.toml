[palgen]
output = "build"

[palgen.extensions]
folders = ["tools"]
inline = false

[project]
name = "librepr"
version = "0.1"
sources = ["include", "benchmark"]

[amalgamate]
root = "repr"
filename = "amalgamated.h"

[benchmark]
output = "build/benchmark"
repeat_each = 5

[graphs]
benchmark_output="build/benchmark"
summary_path="../repr.wiki/"